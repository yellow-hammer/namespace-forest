# src/ — конфигурация для извлечения XSD

Конфигурация 1С с HTTP-сервисом `ВыгрузкаСхем`: `GET /hs/xsd/dump` возвращает JSON
`{ "имя.xsd": "<содержимое>" }` со всеми XSD-схемами платформы. Используется CI.
